export enum CRCLSMessage {
  ERROR = 'error',
  READY = 'ready',
  ACCOUNT_CREATE = 'account-create',
  JOIN = 'join',
  PEER = 'peer',
  MEMBER_CREATE = 'member-create',
  LIST_MEMBERS = 'list-members',
  LIST_CHANNELS = 'list-channels',
  LIST_PEERS = 'list-peers',
  REPLY = 'reply',
}
